pipeline {
    agent any
    tools {
        maven 'Maven3'
    }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build eureka-server') {
            steps {
                sh 'mvn clean package -DskipTests'
            }
        }
    }
}
