# Docker build and runtime configuration
DOCKER_REGISTRY=bpmn
TAG=1.0.0
CONTAINER_NAME=bpmn-process-manager

# Port mappings
FRONTEND_PORT=81

# API Configuration
VITE_API_URL=http://localhost:8080

# Uncomment and set these if you have a backend service
# BACKEND_PORT=8080
# DATABASE_URL=postgresql://user:password@postgres:5432/bpmn_db 