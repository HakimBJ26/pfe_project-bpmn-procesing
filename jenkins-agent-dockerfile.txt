FROM jenkins/agent:latest

USER root

# Install coreutils to ensure cat command is available
RUN apt-get update && \
    apt-get install -y --no-install-recommends coreutils && \
    apt-get clean && \
    rm -rf /var/lib/apt/lists/*

USER jenkins 